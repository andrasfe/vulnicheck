# VulniCheck Environment Variables
# Copy this file to .env and fill in your actual values

#################################
# Authentication (Optional)
#################################

# Google OAuth 2.0 Credentials (required if using --auth-mode google)
# Get these from: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials (Web application)
# 4. Add authorized redirect URI: https://your-domain.com/oauth/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret-here

# For direct Docker use (not using restart script):
# FASTMCP_SERVER_AUTH_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# FASTMCP_SERVER_AUTH_GOOGLE_CLIENT_SECRET=GOCSPX-your-secret-here
# FASTMCP_SERVER_BASE_URL=http://localhost:3000

#################################
# ngrok Configuration (Optional)
#################################

# If using ngrok for HTTPS tunneling (e.g., for OAuth with external services)
# The restart-vulnicheck-ngrok.sh script will use this URL if set,
# otherwise it will auto-detect from ngrok API
#
# Example: NGROK_URL=https://abc123.ngrok-free.dev
# NGROK_URL=

#################################
# Vulnerability Database APIs
#################################

# Optional: NVD API Key for higher rate limits
# Get one free at: https://nvd.nist.gov/developers/request-an-api-key
# Without this key, you're limited to 5 requests per 30 seconds
NVD_API_KEY=your-nvd-api-key-here

# Optional: GitHub token for better rate limits
# Get one at: https://github.com/settings/tokens
GITHUB_TOKEN=your-github-token-here

# Optional: LLM API keys for AI-powered security assessment
# If provided, the MCP passthrough will use LLM to assess risk
# Supports either OpenAI or Anthropic (provide one or the other)
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional: Set request timeout in seconds (default: 30)
REQUEST_TIMEOUT=30

# Optional: Cache TTL in seconds (default: 900)
CACHE_TTL=900

# Optional: Logging configuration
VULNICHECK_LOG_LEVEL=INFO
VULNICHECK_LOG_CONSOLE=false
